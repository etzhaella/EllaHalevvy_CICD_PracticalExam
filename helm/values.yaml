image:
  repository: your-docker-user/flask-monitor
  tag: "latest"
  pullPolicy: IfNotPresent
replicaCount: 1
env:
  AWS_REGION: "us-east-1"
apiVersion: v1
kind: Service
metadata:
  name: myapp
spec:
  selector:
    app: myapp
  ports:
  - port: <Port>
    targetPort: <Target Port>
apiVersion: v1
kind: Service
metadata:
  name: myapp
spec:
  selector:
    app: 
  ports:
  - port: 5001
    targetPort: 5001
